#include <bits/stdc++.h>
#define ll long long int
using namespace std;
int fun(ll a[],ll b[],int i,ll sum)
{
    if()
}
int main()
{
    ll t;
    cin >> t;
    while (t--)
    {
        ll n, m;
        cin >> n >> m;
        ll a[n];
        ll b[n];
        for (int i = 0; i < n; i++)
        {
            cin >> a[i];
        }
        for (int i = 0; i < n; i++)
        {
            cin >> b[i];
        }
        int cnt = 0;
        int i = 0;
        while (i>0)
        {
            /* code */
        }
        
    }

    return 0;
}